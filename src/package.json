{
  "scripts": {
    "build": "rm -rf dist && tsc && node build.js"
  },
  "devDependencies": {
    "@tsconfig/node22": "^22.0.0",
    "adm-zip": "^0.5.16",
    "glob-promise": "^6.0.7",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^3.5.0"
  },
  "main": "hello.js"
}