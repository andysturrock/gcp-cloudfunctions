{
  "scripts": {
    "build": "rm -rf dist && tsc && node build.js"
  },
  "devDependencies": {
    "@tsconfig/node16": "^1.0.3",
    "adm-zip": "^0.5.10",
    "glob-promise": "^6.0.2",
    "typescript": "^5.0.3"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^3.0.0"
  },
  "main": "hello.js"
}
